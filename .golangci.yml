---
run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gocritic
    - goimports
    - gosec
    - govet
    - revive
    - staticcheck

linters-settings:
  goimports:
    local-prefixes: github.com/kinoko-dev/kinoko

issues:
  exclude-dirs:
    - site
    - internal-docs
    - memory
